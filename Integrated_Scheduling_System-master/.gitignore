**/venv/
**/.venv/
**/__pycache__/
**/*.sqlite3

# Environment files with secrets
# NOTE: On server-push branch, .env IS tracked intentionally
# because the server has no file editor access.
# On main branch, .env should NOT be committed.
appointment_scheduling/env

# Keep templates and frontend env files
!.env.server
!frontend/.env
!frontend/.env.production

# Static files collected for production
**/staticfiles/

# Media uploads
**/media/

# OS files
.DS_Store
Thumbs.db